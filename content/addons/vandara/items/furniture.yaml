# Furniture shipped by Vandara. Currently just the Alchemy Table —
# crafted by act-build-workshop, placed in a home via the housing
# system, gates the Workshop section's recipe buttons (greyed out
# while not placed). Modifiers are intentionally empty: the table's
# only "stat effect" is enabling alchemy; that's surfaced via the
# section's recipeLockedUnlessPlaced check, not via the pipeline.

- id: item-alchemy-table
  title: item_alchemy_table_title
  desc: item_alchemy_table_desc
  image: img/furniture/stove.webp # placeholder — replace with a real alchemy table sprite later
  consumable: false
  category: furniture
  spaceCost: 2
